# Core numerical + ML
numpy
scipy
torch

# TorchSig (prefer local submodule for reproducible dev)
# install with `uv sync` to use `third_party/torchsig` path source from pyproject.toml
torchsig

# Config + serialization
pyyaml

# GUI (pick one, see below)
# Option A: simple + fast
streamlit

# Option B: more control (desktop app)
# PyQt6

# Data handling
pandas

# Visualization (for previews later)
matplotlib

# Utility
tqdm
